/* Background canvas styling (ensure it stays behind content) */
#bg {
  position: fixed !important;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  display: block;
  z-index: -5 !important; /* behind everything */
  pointer-events: none;
}

/* ensure your content sits above */
.content {
  position: relative;
  z-index: 10 !important;
}
