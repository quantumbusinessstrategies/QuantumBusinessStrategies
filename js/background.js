const canvas=document.getElementById('bg');const ctx=canvas.getContext('2d');canvas.width=innerWidth;canvas.height=innerHeight;let t=0;const particles=[];for(let i=0;i<500;i++){particles.push({x:Math.random()*canvas.width,y:Math.random()*canvas.height,vx:(Math.random()-0.5)*1.5,vy:(Math.random()-0.5)*1.5,size:Math.random()*3+1,hue:Math.random()*360});}function draw(){ctx.fillStyle='rgba(0,0,0,0.05)';ctx.fillRect(0,0,canvas.width,canvas.height);const cx=canvas.width/2;const cy=canvas.height/2;t+=0.8;const hue=t%360;ctx.lineWidth=1.2;for(let row=-30;row<30;row++){for(let col=-30;col<30;col++){const x=cx+col*60*1.5+(row%2)*30*0.75;const y=cy+row*60*0.866;if(x>-200&&x<canvas.width+200&&y>-200&&y<canvas.height+200){ctx.strokeStyle=`hsla(${hue},80%,70%,0.3)`;ctx.beginPath();for(let i=0;i<6;i++)ctx.lineTo(x+30*Math.cos(i*Math.PI/3),y+30*Math.sin(i*Math.PI/3));ctx.closePath();ctx.stroke();}}}particles.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>canvas.width)p.vx*=-1;if(p.y<0||p.y>canvas.height)p.vy*=-1;ctx.fillStyle=`hsla(${p.hue},100%,80%,0.9)`;ctx.beginPath();ctx.arc(p.x,p.y,p.size,0,Math.PI*2);ctx.fill();p.hue=(p.hue+1.8)%360;});requestAnimationFrame(draw);}draw();window.addEventListener('resize',()=>{canvas.width=innerWidth;canvas.height=innerHeight;});